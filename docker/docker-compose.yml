services:
  kace-dev:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: kace-dev-container
    volumes:
      - ..:/workspace
    ports:
      - "7125:7125"
    stdin_open: true
    tty: true
    environment:
      - PYTHONUNBUFFERED=1
      - KACE_DOCKER=1
